import React from "react";
import ReactDOM from "react-dom/client";
import ClinicEMR from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ClinicEMR />
  </React.StrictMode>
);
